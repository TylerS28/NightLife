function SetPedFlag(object, flag, enabled)
    exports['srp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['srp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['srp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['srp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['srp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['srp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['srp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['srp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['srp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['srp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['srp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['srp-flags']:SetObjectFlags(object, flag)
end